name: Android compile minimal
on: pull_request
permissions:
  contents: read
jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false
      - run: ./gradlew compileDebugKotlin --quiet
        working-directory: mobile/android
